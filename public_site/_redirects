# Netlify -> GitHub Pages forwarder.
#
# GitHub Pages is now the canonical host for these boards (free, unmetered, no build
# credits — Netlify began skipping every deploy with "account credit usage exceeded"
# on Aug 3 2026). A *.netlify.app subdomain cannot itself be repointed at Pages, so
# instead every path 301s to the Pages equivalent and old links keep working.
#
# NOTE: this only takes effect once Netlify is able to build again. While the account
# is over its credit limit all deploys are skipped, so the forwarder stays dormant.
#
# To undo (e.g. to serve from Netlify again), delete this file and redeploy.

/*    https://aliu2298.github.io/edge-machine/:splat    301!
